import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  spinner: {
    position: 'fixed',
    left: '50%',
    top: '50%',
  },
}));

export default useStyles;
