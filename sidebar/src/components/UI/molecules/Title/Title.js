import React from 'react';
import Span from '../../atoms/span/Span';

const Title = () => {
  const style = {
    padding: '15px',
  };
  return <Span style={style} content="내 목표" />;
};

export default Title;
