import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  dialogContent: {
    width: '400px',
  },
  subText: {
    fontSize: '13px',
  },
}));

export default useStyles;
