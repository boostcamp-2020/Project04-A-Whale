import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  noContent: {
    position: 'fixed',
    left: '36%',
    top: '61%',
    fontSize: 17,
    color: '#999',
  },
}));

export default useStyles;
